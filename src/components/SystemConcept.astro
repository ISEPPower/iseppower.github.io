---
const steps = [['Inputs / Context', 'Consumption, flexibility, price signals and grid conditions.'], ['Knowledge Base', 'Domain concepts, constraints and candidate rules.'], ['Reasoning / Inference', 'Evaluate the context against the available knowledge.'], ['Recommendation', 'Suggest an action with a clear explanation.']];
---
<ol class="system-flow">{steps.map(([title, description], i) => <li><span class="step-label">0{i + 1}<span aria-hidden="true"> →</span></span><h3>{title}</h3><p>{description}</p></li>)}</ol><p class="caption">Conceptual architecture · Inputs, representation and inference method are to be defined.</p>
